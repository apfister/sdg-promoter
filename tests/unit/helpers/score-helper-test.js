
import { scoreHelper } from 'sdg-promoter/helpers/score-helper';
import { module, test } from 'qunit';

module('Unit | Helper | score helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scoreHelper([42]);
  assert.ok(result);
});

