import stripTags from 'sdg-promoter/utils/strip-tags';
import { module, test } from 'qunit';

module('Unit | Utility | strip tags');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stripTags();
  assert.ok(result);
});
